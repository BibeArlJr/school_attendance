<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <!--
            Prompt 46: deliberately NOT overriding DB_CONNECTION/DB_DATABASE
            here — a real Postgres test database (school_erp_test, see
            .env.testing) is required, not SQLite in-memory, since this
            app relies on real Postgres features (Prompt 5's partial
            unique index on is_primary_contact, JSON columns) that SQLite
            doesn't correctly exercise. APP_ENV=testing below makes
            Laravel load .env.testing, which supplies DB_* pointing at
            school_erp_test — never the school_erp_test dev database.
        -->
        <env name="APP_ENV" value="testing"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
        <env name="NIGHTWATCH_ENABLED" value="false"/>
    </php>
</phpunit>
